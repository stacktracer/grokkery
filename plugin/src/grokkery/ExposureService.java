package grokkery;

public class ExposureService
{
    
    public void expose(Object object, String name)
    {
        System.err.println("Exposing: object = " + object + ", name = " + name);
    }
    
}
